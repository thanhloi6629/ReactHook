import React from 'react'
import { useMemo } from 'react';

const fiexdFirstRowColumn = () => {

  const tblRows = useMemo(() => ['日付', '男性', '女性', '計', '', '初回', '2回目', '3回目', '常連', '計', '', '回数', '金額'], []);
    
    return (
        <div>
            
        </div>
    )
}

export default fiexdFirstRowColumn
