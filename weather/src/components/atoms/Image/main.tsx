import { StyleProps } from 'types/StyleProps';
import React, { Fragment } from 'react';

type ImageProps = {
  src: string;
  alt: string;
  styleProps: StyleProps;
};
const main = () => (
  <>
    loi
  </>
);

export default main;
